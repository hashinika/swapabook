

import {get, patch, post, put, remove, getCurrentLocation} from './request.helpers';
import {setStorageValue, getStorageValue, removeStorageValue} from './storage.helpers';

export {
  get,
  patch,
  post,
  put,
  remove,
  getCurrentLocation,
  setStorageValue,
  getStorageValue,
  removeStorageValue,
}